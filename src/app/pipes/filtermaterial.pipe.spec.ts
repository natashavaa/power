import { FiltermaterialPipe } from './filtermaterial.pipe';

describe('FiltermaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltermaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
