import { FilterconsultasPipe } from './filterconsultas.pipe';

describe('FilterconsultasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterconsultasPipe();
    expect(pipe).toBeTruthy();
  });
});
